[x] 1. Update `package.json` to include Cursor-specific context keys in the `when` clause for `noSendEnter.newline` and `noSendEnter.send` commands.
[x] 2. Build the extension using `npm run package` (or compile and let user debug).
[/] 3. Ask user to test in Cursor.
